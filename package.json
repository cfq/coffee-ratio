{
  "name": "coffeeratio",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "sass": "^9.0.0"
  },
  "dependencies": {
    "vite": "^7.0.5"
  }
}
